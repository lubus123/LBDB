[package]
name = "td-gammon"
version = "0.1.0"
edition = "2021"

[dependencies]
rand = "0.8"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
# rayon = "1.10"  # forward pass too small for thread overhead

[profile.release]
opt-level = 3
lto = true
